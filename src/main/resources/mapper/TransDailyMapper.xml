<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--对应mapper接口 -->
<mapper namespace="com.webank.webase.node.mgr.transdaily.TbTransDailyMapper">
  <resultMap id="seventDayOfTransMap" type="com.webank.webase.node.mgr.transdaily.SeventDaysTrans">
    <id column="network_id" javaType="java.lang.Integer" jdbcType="INTEGER" property="networkId"/>
    <id column="trans_day" javaType="java.time.LocalDate" jdbcType="TIMESTAMP" property="day"/>
    <result column="trans_count" javaType="java.lang.Integer" jdbcType="INTEGER"
      property="transCount"/>
  </resultMap>

  <select id="listSeventDayOfTransDaily" resultMap="seventDayOfTransMap">
		select network_id,trans_day,trans_count from tb_trans_daily where network_id = #{networkId} and trans_day &gt;= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
	</select>

  <update id="updateTransDaily">
		update tb_trans_daily set trans_count=#{transCount},block_number=#{latestBlockNumber},modify_time=now()
		where network_id=#{networkId} and trans_day=#{transDay} and block_number=#{oldBlockNumber}
	</update>

  <insert id="addTransDailyRow" parameterType="com.webank.webase.node.mgr.transdaily.TbTransDaily">
		insert ignore into tb_trans_daily(network_id,trans_day,trans_count,block_number,create_time,modify_time)
		values(#{networkId},#{transDay},#{transCount},#{blockNumber},NOW(),NOW())
	</insert>

  <select id="queryMaxBlockByNetwork" parameterType="java.lang.Integer"
    resultType="java.math.BigInteger">
		select max(block_number) from tb_trans_daily where network_id = #{networkId}
	</select>
</mapper>